// Main window for Unpackrr-rs
// Fluent Design styled BA2 batch unpacker

export component MainWindow inherits Window {
    title: "Unpackrr - BA2 Batch Unpacker";
    preferred-width: 1000px;
    preferred-height: 700px;
    min-width: 1000px;
    min-height: 700px;

    background: #f3f3f3; // Light theme background

    // Placeholder - will be implemented in Phase 1.7
    Text {
        text: "Unpackrr-rs - Initializing...";
        font-size: 24px;
        color: #000;
        horizontal-alignment: center;
        vertical-alignment: center;
    }
}
